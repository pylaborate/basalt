'''`pylaborate` namespace package, via the `pyalborate.basalt` distribution

## Overview

The `pylaborate` package represents a namespace package.

This definition of the namespace package is provided via the `pylaborate.basalt`
distribution.
'''

from pkg_resources import declare_namespace
declare_namespace(__name__)
